{
  "name": "tech-challenge-2",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "preinstall": "pnpm install --ignore-scripts",
    "start": "pnpm --parallel --filter tech-challenge-2_* dev",
    "build": "pnpm --parallel --filter tech-challenge-2_* build",
    "serve": "pnpm --parallel --filter tech-challenge-2_* start"
  }
}
